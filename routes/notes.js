const notes = require('express').Router();

// require helper functions

// get notes
notes.get('/', (req, res) => {
    // add function to read .json file
});

// post new note
notes.post('/', (req, res) => {
    // add function to add to .json file
})


module.exports = notes;